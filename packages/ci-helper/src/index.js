const bin = require('./bin');
const eslint = require('./eslint');
const prettier = require('./prettier');
const webpack = require('./webpack');
const babel = require('./babel');

module.exports = { bin, eslint, prettier, webpack, babel };
